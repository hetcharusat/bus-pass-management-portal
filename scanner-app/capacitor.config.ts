import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tirth.buspassscanner',
  appName: 'Dhanlaxmi Bus Pass',
  webDir: 'dist'
};

export default config;
